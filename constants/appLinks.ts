/** Public URLs and contact targets for Settings support rows. */
export const PRIVACY_POLICY_URL = "https://lowalk.app/privacy";

export const FEEDBACK_EMAIL = "contact@lowalk.app";

export const FEEDBACK_MAILTO = `mailto:${FEEDBACK_EMAIL}?subject=${encodeURIComponent("Lowalk feedback")}`;
